Sources for the plugin `shared.google.play.services.ads`.

Add following to your `build.settings` to use:
```lua
{
    plugins = {
        "shared.google.play.services.ads" = {
            publisherId = "com.coronalabs",
        },
    },
}
```
